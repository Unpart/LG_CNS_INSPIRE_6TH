<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.inspire_mybatis.features.blogs.repository.BlogMapper">

    <!-- findAll --> 

    <select id="findByAll" 
            resultType="com.example.inspire_mybatis.features.blogs.domain.BlogResponseDTO">
        SELECT ID, TITLE, CONTENT, CATEGORY, EMAIL
        FROM SPRING_BLOG_TBL

    </select>

    <!-- save --> 

    <insert id="save" parameterType="com.example.inspire_mybatis.features.blogs.domain.BlogRequestDTO">
        INSERT INTO SPRING_BLOG_TBL (ID, TITLE, CONTENT, CATEGORY, EMAIL)
        VALUES (NEXTVAL(BLOG_SEQ), #{title}, #{content}, #{category}, #{email})
    </insert>

    <!-- findById --> 

    <select id="findById" 
            parameterType="java.lang.Integer"
            resultType="com.example.inspire_mybatis.features.blogs.domain.BlogResponseDTO">
        SELECT ID, TITLE, CONTENT, CATEGORY, EMAIL
        FROM SPRING_BLOG_TBL
        WHERE ID = #{id}
    </select></mapper>       